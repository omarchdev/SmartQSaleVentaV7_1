package com.omarchdev.smartqsale.smartqsaleventas.Activitys;

public class SnackBarLoading {

/*
    Context context;
    View view;

    public SnackBarLoading(Context context,View view) {
        this.context=context;
        this.view=view;

    }

    public SnackBarLoading() {
        this.context=context;
        this.view=view;
    }
    public void setData(Context context,View view){
        this.context=context;
        this.view=view;
    }
    public void MostrarSnackBar(String texto){
        if(context!=null && view!=null) {
            snackbar = Snackbar.make(view, texto, Snackbar.LENGTH_INDEFINITE);
            ViewGroup content=(ViewGroup)snackbar.getView().findViewById(android.support.design.R.id.snackbar_text).getParent();
            ProgressBar item = new ProgressBar(context);

            content.addView(item);

            snackbar.show();
        }
    }

    public void Ocultar(){
        if(snackbar!=null)
        snackbar.dismiss();
    }
*/
}
