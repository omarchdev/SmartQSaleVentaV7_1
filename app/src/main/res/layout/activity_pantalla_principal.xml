<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

        <include
            layout="@layout/app_bar_pantalla_principal"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />


    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        style="@style/AppTheme.NavigationView.Principal"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_pantalla_principal"
        app:itemShapeAppearanceOverlay="@style/ShapeAppearance.App.SmallComponent"
        app:itemShapeFillColor="@color/nav_item_background_state"
        app:itemShapeInsetStart="16dp"
        app:itemShapeInsetEnd="16dp"
        app:itemHorizontalPadding="12dp"
        app:itemIconPadding="24dp"
        app:subheaderColor="#99000000"
        app:subheaderInsetStart="12dp"
        app:itemVerticalPadding="12dp"
        app:menu="@menu/activity_pantalla_principal_drawer">

        <LinearLayout
            android:id="@+id/layout_footer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical"
            android:paddingStart="32dp"
            android:paddingEnd="32dp"
            android:paddingBottom="16dp">

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:layout_marginBottom="8dp"
                android:background="#1A000000" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnCerrarSesion"
                style="@style/AppTheme.Button.CerrarSesion"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="start|center_vertical"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:text="Cerrar Sesión"
                android:textAllCaps="false"
                app:icon="@drawable/open_door"
                app:iconGravity="textStart" />
        </LinearLayout>

    </com.google.android.material.navigation.NavigationView>



</androidx.drawerlayout.widget.DrawerLayout>
